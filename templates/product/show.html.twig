{% extends 'base.html.twig' %}
{% block title %}{{ product.name }} - Sound Studio{% endblock %}
{% block content %}

    <div class="row">
        <div class="col-md-4">
            <img src="/uploads/{{product.illustration}}" alt="{{ product.name }}" class ='img-fluid'>
        </div>
    <div class="col-md-7 my-auto">
        <h3>{{ product.name }}</h3>
        <p>{{ product.subtitle }}</p>
        <span class="product-page-price">{{ (product.price/100)|number_format(2,',','.') }} €</span>
        <hr>
        <p>{{ product.description }}</p>
        <a href="{{path('app_add_to_cart',{'id' : product.id})}}" class="btn btn-dark">Ajouter au panier</a>
    </div>
    </div>
    <!--
        <hr>
    <h3>Titre </h3>
    <p>Paragraphe</p>
    </div>

    -->

{% endblock %}
