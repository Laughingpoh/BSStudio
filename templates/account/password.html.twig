{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe - Sound Studio{% endblock %}

{% block content %}
    <h1>Modification de mot de passe</h1>
    <hr>
    {%if notification %}
        <div class="alert alert-info">{{ notification }}</div>
    {%endif%}
    {{ form(form)}}
    <a href="{{path('account')}}" class="btn btn-sm btn-dark btn-block mt-3">Retour</a>
    <hr>


{% endblock %}
